@if (Model != null)
{
    <div class="text-center">
        @if (Model.Length > 0)
        {
            @Model.Length
            <span>bytes</span>
        }
        else
        {
            <span></span>
        }
    </div>
}